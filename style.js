import { StyleSheet } from "react-native"

export default ExStyles=StyleSheet.create({
    textBox:{
      color: 'green',
      fontSize: 25,
      backgroundColor: "#fff",
      borderColor: 'black',
      borderWidth: 5,
    }
  })